[Unit]
Description=Provision gpg-agent SSH forwarding (socket dir + sshd StreamLocalBindUnlink)
Documentation=man:gpgconf(1)

[Service]
Type=oneshot
Environment=GNUPGHOME=%h/.config/gnupg
ExecStart=%h/.local/libexec/gpg-agent-forward-provision
RemainAfterExit=yes

[Install]
WantedBy=default.target
