# Gofile — Go binaries installed via `go install`.
# Entries are full module paths (NOT bare binary names).
# Synced by `system-package go sync`. Strict cleanup: removing a
# line uninstalls the binary on next sync.

# ai-assist-input — prefilled textarea popup for the ai-assist terminal
# assistant (Enter submits, Shift+Enter newlines).
github.com/Townk/ai-assist-input

# ai-assist-pager — interactive TUI markdown viewer for the docked agent pane
# (no-wrap code/tables with horizontal scroll).
github.com/Townk/ai-assist-pager

{{ if eq .profile "personal" -}}
github.com/bscott/pm-cli/cmd/pm-cli
{{- end }}

# vim:ft=conf.gotmpl
