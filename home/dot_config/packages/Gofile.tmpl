# Gofile — Go binaries installed via `go install`.
# Entries are full module paths (NOT bare binary names).
# Synced by `system-package go sync`. Strict cleanup: removing a
# line uninstalls the binary on next sync.

# ai-playbook — the AI terminal assistant and themed input-widget backend
# (input::confirm/line/choose/form shim over `ai-playbook input`, the docked
# pager, and the diff viewer — all subcommands of the one binary; installs the
# short `apb` alias too via cmd/apb).
github.com/Townk/ai-playbook/cmd/ai-playbook
github.com/Townk/ai-playbook/cmd/apb

{{ if eq .profile "personal" -}}
github.com/bscott/pm-cli/cmd/pm-cli
{{- end }}

# vim:ft=conf.gotmpl
