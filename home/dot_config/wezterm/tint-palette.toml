# WezTerm window-tint palette: name -> hex. SINGLE SOURCE OF TRUTH, read by
# wezterm.lua (the tint renderer) and by `system-onboard` (color set/list/
# validate). Flat `name = "#rrggbb"`; each is a faint wash over the Catppuccin
# Mocha base (#1e1e2e) — noticeable at a glance but unobtrusive. Add a row to
# offer a new tint; reference it from a machine's onboard color or a profile
# default (lib/terminal-location.zsh).
cyan   = "#1c232f"
blue   = "#1d1f33"
amber  = "#2a241f"
teal   = "#16302b"
purple = "#241c30"
grey   = "#2a2a32"
